### PLANTILLA PARA WINDOWS

from qiskit import QuantumRegister, ClassicalRegister, QuantumCircuit, transpile
from qiskit.circuit.library.standard_gates import *
from qiskit_aer import AerSimulator
from qiskit.visualization import plot_histogram
from qiskit.quantum_info.operators import Operator
from math import pi
import numpy 
from qiskit.visualization import plot_state_city 
from qiskit.visualization import plot_histogram
from qiskit.circuit.library import MCMT
from qiskit.circuit.library import UnitaryGate


#DECLARATION#
#INITIALIZATION#
#CALCULUS#
#MEASURES#

# For execution
simulator = AerSimulator()
# optimization_level=0: con oraculos anidados el circuito se descompone en cientos de
# miles de puertas y las pasadas de optimizacion tardan mas que la propia simulacion
# (20s frente a 2s). Como el destino es un simulador, optimizar no aporta nada.
compiled_circuit = transpile(circuit, simulator, optimization_level=0)
result = simulator.run(compiled_circuit, shots=#SHOTS#).result()
histogram = result.get_counts()


#backend = Aer.get_backend('statevector_simulator')
#job = backend.run(circuit, shots=100) 
#result = job.result()
#histogram = result.get_counts()

#outputstate = result.get_statevector(circuit, decimals=1) 

print(histogram)

#plot_histogram(histogram)
#print(circuit)
#with open('C:/Program Files/Apache Software Foundation/Tomcat 9.0/temp/o.txt', 'w') as file:
#     file.write(json.dumps(histogram))